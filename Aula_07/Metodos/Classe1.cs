namespace Metodos
{
    internal class Classe1
    {
        //Métodos criados -- Serão lidos no método 'Main'.
        public static double Somar(double x, double y)
        {
            double resultado;

            return resultado = (x + y);
        }

        public static void MostrarNoConsole(double mensagem)
        {
            Console.WriteLine(mensagem);
        }
    }
}
